local options = {
  theme = 'hyper',
  disable_move= false,
  shortcut_type = "number",
  change_to_vcs_root = false,
  config = {},
  hide = {
    statusline = true,
    tabline = true,
    winbar = true,
  },
  preview = {
    command = true,
    file_path = false,
    file_height = true,
    file_width = false,
  },
}

return options
